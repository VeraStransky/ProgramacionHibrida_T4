import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.verastranskyt4',
  appName: 'verastranskyt4',
  webDir: 'www/browser',
  server: {
    androidScheme: 'https'
  }
};

export default config;
